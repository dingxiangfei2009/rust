- Always use `./x b --stage 1` when building the rustc compiler to ensure both the compiler and the standard library are built correctly for testing.
- The implementation plan MUST be checked as part of the initial critical instructions. Check it religiously against the intention behind every change.
- If something violates the assumptions or axioms in the implementation plan, ask for user prompt with an explanation of the situation and what you are struggling with.
- Do not allow any LLVM spilling in coroutine code. If you propose to allow spilling due to increased coroutine use, I will reject the proposal without hesitation; you must find a workaround to avoid spilling at all cost.
- Zero overhead as a design principle is non-negotiable.
- Never use raw pointer arithmetic (`Rvalue::BinaryOp(BinOp::Offset, ...)`) or hardcoded manual byte offset calculations in compiler-generated MIR shims (`shim.rs`). Always access coroutine fields and upvars via high-level semantic MIR projections (`((*ptr) as variant).field`) to maintain flexibility and high-level separation of concerns as the layout engine evolves.
- When the agent needs human input, explain all identifiers, where they are coming from, what their types are; show both the hyperlink to the lines in the relevant files and the surrounding IR text wherever applicable.
- Distinguish strictly between byte offsets (memory offsets in LLVM IR / ABI layouts, measured in bytes) and MIR field indices (`FieldIdx`, struct field numbers `0, 1, 2...`). Byte offsets are not field indices; never confuse or interchangeably refer to byte offsets (e.g., offset 0 or offset 8) and `FieldIdx` when reasoning about aggregate structures or projections.
- Before reporting success, always run ALL `tests/ui` and `src/tools/miri` tests to verify your changes.
- NEVER use `println!` tracing or blind `compiletest` retries to debug runtime crashes (`SIGSEGV`, core dumps, or ABI violations) in compiled tests. STOP adding `println!` statements and immediately use `rust-gdb -batch` directly on the compiled binary (e.g., `rust-gdb -batch -ex "run" -ex "x/10i \$pc - 15" -ex "info registers" --args <binary>`) to inspect exact hardware register states and disassembly around `$pc`.
- If you hit unexpected test failures, crashes, or feel stuck after an initial debugging attempt, immediately pause and report to the user what you are struggling with—providing concrete symptoms, IR observations, and working hypotheses—before attempting further silent retries.
- To strictly enforce zero overhead, in `compiler/rustc_codegen_ssa/src/mir/mod.rs`, `size` and `align` passed to `coro_id_retcon` MUST remain `let size = start_bx.const_u32(0);` and `let align = start_bx.const_u32(1);`. Do not ever change `size` or `align`; this is part of the design and is non-negotiable.
- To strictly enforce zero overhead, retcon inline storage size (`Id->getStorageSize()`) is locked to `0` at all times as an uncompromisable tripwire against spilling. If a retcon coroutine triggers `coroutine_alloc_panic` or manifests as a `SIGSEGV`, it indicates that LLVM produced a non-empty coroutine frame (`B.getStructSize() > 0`).
- A runtime panic `coroutine frame exceeded LLVM buffer size` (`coroutine_alloc_panic`) or `SIGSEGV` means that we did not capture all saved locals, or LLVM passes accidentally moved values around so that they incorrectly become locals that have to be spilled onto the coroutine detached frame (`B.getStructSize() > 0`).
- When investigating `coroutine frame exceeded LLVM buffer size` (`coroutine_alloc_panic`), investigation MUST focus on LLVM passes that are run from the IR initially built by `rustc_codegen_llvm` and all the intermediate IRs.
- You must diagnose and eliminate the stray local or spill causing frame allocation rather than increasing storage size or allowing fallbacks.
- `StorageLive` and `StorageDead` MIR statements (`@llvm.lifetime.start.p0` / `@llvm.lifetime.end.p0` LLVM intrinsics) do NOT influence `CoroSplitPass` liveness analysis or coroutine frame allocation (`%.coro.frame`). Never attempt to add, remove, or rearrange `StorageLive` / `StorageDead` statements to fix `coroutine_alloc_panic` or prevent coroutine frame spilling. To eliminate a spill in `CoroSplitPass`, you must eliminate the cross-call SSA definition-use chain across `@llvm.coro.suspend.retcon` (e.g., by recomputing the value after suspension or utilizing `!coro.outside.frame` allocas with `volatile` read/writes).
- When the user asks for a response or report in an artifact, unless moving onto implementation, always update and respond in the same artifact rather than creating new artifacts or answering purely in chat.
- When updating an artifact, preserve all existing valid information and sections. Only edit content to reconcile conflicts or correct wrong conclusions, or append new sections to address new questions, so that prior verified context and diagnostic findings are never lost.
- Always prefer relative paths (`./x ...`, `build/...`, `src/...`) over absolute paths (`/usr/local/google/home/...`) when invoking terminal commands inside the workspace.
- `rust-gdb` must ONLY be used for running and debugging code produced by `rustc` (e.g., inspecting compiled binaries, register states, disassembly, backtraces, and memory layouts). Never abuse `rust-gdb -batch -ex 'python ...'` as an arbitrary program execution engine, general Python script runner, or filesystem utility.
- Always run `rust-gdb -batch` (and other debugger sessions) with a strict timeout using internal tools: whenever a `rust-gdb` command transitions to a background task (`task-XXXX`) in `run_command`, immediately invoke the `schedule` tool to set a one-shot timer (`DurationSeconds=30` or `60`) with `TimerCondition="task-XXXX"`. If the timer fires because `rust-gdb` is looping indefinitely (`commands ... continue`) or hanging, immediately terminate the stuck task using `manage_task(Action: "kill", TaskId: "task-XXXX")`.

